# Mailman Agents

**The AI orchestrator that lives in your browser (and doesn't cost a dime).**

> Tired of paying for API calls? Mailman takes the AI accounts you already have and wires them together—no backend, no fees, just your browser tabs doing the heavy lifting.

---

## Wait, what does this actually do?

Mailman is a browser extension that runs a team of AI agents entirely on your machine. Instead of routing everything through a paid cloud API, it works with whatever AI interfaces you're already logged into (ChatGPT, Claude, etc.).

It automates the annoying back-and-forth: one agent plans, another builds, another checks for bugs—and passes the context between them invisibly. You just get the final result.

**No hidden costs. No tracking. Just your local browser doing the job.**

---

## How it works (the short version)

1. **It hooks into your open tabs** – Mailman sees which AI services you're currently authenticated on.
2. **It builds a task queue** – You give it a goal, it breaks it down into steps.
3. **It passes prompts between agents** – No copy-pasting manually between tabs. It just does it.
4. **You get the final output** – Consolidated and dropped right into your workspace.

---

## The 7 agents (because yes, it's a whole team)

| Agent | What they actually do |
| :--- | :--- |
| **The Architect** | Figures out the structure so you don't end up with a mess. |
| **The Specialist** | Writes the actual code/solution based on that plan. |
| **The Librarian** | Checks if the approach is outdated or breaks real-world constraints. |
| **The Red Teamer** | Tries to break the solution—finds edge cases and weird bugs. |
| **The Optimizer** | Cleans up the mess, makes it faster and leaner. |
| **The Explainer** | Adds docs so you actually understand what it's doing. |
| **The Synthesizer** | Stitches everything together into one final artifact. |

---

## How to install it

Since this runs 100% locally (privacy first), you'll have to load it manually via Developer Mode. Takes 2 minutes:

1. Clone or download this repo to your machine.
2. Open your browser's extensions page (`chrome://extensions/` or `edge://extensions/`).
3. Toggle **Developer Mode** on (top right).
4. Click **Load unpacked** and select the `mailman-agents` folder.
5. Pin it to your toolbar and you're good to go.

---

## Want to support the project?

Mailman is open-source, free, and we're not tracking a single thing. If this saves you from a hefty cloud bill, and you're feeling generous—buy us a coffee (or three).

**EVM (ETH / BNB / Polygon / Base):**  
`0xEE725966cEcd2014c6191C100f927485F0636BA3`

**Solana (SOL / USDT):**  
`2a7kHeVRsXJPojJyGKqqDfpd1Vd1wnzHY8drPBKx7uiq`

---

## License

MIT – do whatever you want with it.
